// Basic attributes export if needed, or just dummy to satisfy main export
// Currently HtmlParser handles attributes internally.
// We can export CSSStyle here if users want to access it?
export 'parser/css_style.dart';
